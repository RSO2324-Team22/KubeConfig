apiVersion: apps/v1
kind: Deployment
metadata:
  name: planning-service
  namespace: default
  labels:
    app: planning-service
spec:
  replicas: 5
  selector:
    matchLabels:
      app: planning-service
  template:
    metadata:
      name: planning-service
      namespace: default
      labels:
        app: planning-service
    spec:
      containers:
        - name: planning-service
          image: "pusti/rso2324-team22-planning-service:latest"
          ports:
            - containerPort: 8080
              name:  http
          env:
            - name: CONNECTIONSTRINGS__PLANNINGDATABASE
              valueFrom:
                secretKeyRef:
                  name: postgres-secrets
                  key: planning_service_connection_string
            - name: KAFKA_URL
              value: "$(KAFKA_SERVICE_HOST):$(KAFKA_SERVICE_PORT)"
            - name: SERILOG__WRITETO__0__ARGS__NAME
              value: "Graylog"
            - name: SERILOG__WRITETO__0__ARGS__HOSTNAMEORADDRESS
              value: "$(GRAYLOG_SERVICE_HOST)"
            - name: SERILOG__WRITETO__0__ARGS__PORT
              value: "$(GRAYLOG_SERVICE_PORT)"
            - name: SERILOG__WRITETO__0__ARGS__TRANSPORTTYPE
              value: "Udp"

---

apiVersion: v1
kind: Service
metadata:
  name: planning-service
spec:
  type: ClusterIP
  selector:
    app: planning-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: http
